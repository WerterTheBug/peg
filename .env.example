# Frontend API base URL for production/static hosts (Netlify, etc.)
# Leave empty in local development to use Vite proxy.
VITE_API_BASE_URL=""

# Backend cloud database connection string (used by Node server)
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority"
